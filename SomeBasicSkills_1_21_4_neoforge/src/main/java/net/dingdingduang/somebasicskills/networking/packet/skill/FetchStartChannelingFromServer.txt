package net.dingdingduang.somebasicskills.networking.packet.skill;

import net.minecraft.network.FriendlyByteBuf;
import net.minecraft.world.entity.player.Player;
import net.minecraftforge.network.NetworkEvent;

import java.util.function.Supplier;

import static net.dingdingduang.somebasicskills.globalmethods.GeneralMethods.getMinecraftInstance;
import static net.dingdingduang.somebasicskills.gui.overlay.SkillChannelingOverlay.getSkillChannelingOverlayInstance;

public class FetchStartChannelingFromServer {
    private final String SkillID;

    public FetchStartChannelingFromServer(String skillID) {
        this.SkillID = skillID;
    }

    public FetchStartChannelingFromServer(FriendlyByteBuf buf) {
        this.SkillID = buf.readUtf();
    }

    public void toBytes(FriendlyByteBuf buf) {
        buf.writeUtf(this.SkillID);
    }

    public boolean handle(Supplier<NetworkEvent.Context> ctx) {
        ctx.get().enqueueWork(() -> {
            //do shiet on client side
            Player cp1 = getMinecraftInstance().player;

            getSkillChannelingOverlayInstance().setChannelingTimer(true, cp1, this.SkillID);
        });

        return true;
    }
}
